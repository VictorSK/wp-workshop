# frozen_string_literal: true

source 'https://rubygems.org'

# Use Github as a source for gems
git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Gems used in the project

# Used for Sass/SCSS
gem 'sass', '~> 3.7.4'

# Use the proper Ruby Gem for your version of Bootstrap.
# Only use the specific version you need for this project.
gem 'bootstrap', '~> 5.2.3' # Bootstrap version 5
# gem 'bootstrap', '~> 4.6.2' # Bootstrap version 4
# gem 'bootstrap-sass', '~> 3.3.7' # Bootstrap version 3